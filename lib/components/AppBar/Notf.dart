import 'package:flutter/material.dart';

class Notf extends StatelessWidget {
  const Notf({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
        onPressed: null,
        icon: Icon(
          Icons.notification_add,
          color: Colors.black,
        ));
  }
}
